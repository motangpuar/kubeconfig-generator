apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{USERNAME}}-cluster-viewer
subjects:
- kind: User
  name: "{{EMAIL}}"
  apiGroup: rbac.authorization.k8s.io
roleRef:
  kind: ClusterRole
  name: cluster-viewer-{{USERNAME}}
  apiGroup: rbac.authorization.k8s.io
